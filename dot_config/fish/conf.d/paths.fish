# ── paths.fish ──────────────────────────────────────────────
# fish_add_path is idempotent — safe to re-source

fish_add_path $HOME/.cargo/bin
fish_add_path $HOME/.npm/bin
fish_add_path $HOME/.local/bin/scripts
